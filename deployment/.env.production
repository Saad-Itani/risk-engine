# Risk Engine - Production Environment Variables
# Copy this file to /opt/risk-engine/.env.production on the VPS

# ============================================================================
# Flask Configuration
# ============================================================================
FLASK_ENV=production

# Database Configuration
# IMPORTANT: Use absolute path on VPS (/var/lib/risk-engine/risk_engine.db)
DATABASE_URL=sqlite:////var/lib/risk-engine/risk_engine.db

# ============================================================================
# Backtesting Parameters
# ============================================================================

# Default backtesting lookback period (trading days)
DEFAULT_BACKTEST_LOOKBACK=252

# Default backtesting step size (evaluate every N days)
DEFAULT_BACKTEST_STEP=5

# Number of recent breaches to return in API response
BACKTEST_RECENT_BREACHES_LIMIT=10

# Kupiec test p-value threshold for model calibration
KUPIEC_PVALUE_THRESHOLD=0.05

# ============================================================================
# LLM Recommendations (OpenAI) - Optional
# ============================================================================

# OpenAI API Key (leave empty for now, add later if needed)
OPENAI_API_KEY=

# OpenAI Model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# OpenAI Base URL (optional - for custom endpoints like gpt-5-nano)
OPENAI_BASE_URL=

# LLM generation parameters
LLM_MAX_TOKENS=1000
LLM_TEMPERATURE=0.3

# ============================================================================
# Notes
# ============================================================================
# - After copying this file to the VPS, you can customize these values
# - The OPENAI_API_KEY is optional - LLM features will be disabled if not set
# - Database path must be absolute when using sqlite:////
# - All other settings have sensible defaults and don't need changes
